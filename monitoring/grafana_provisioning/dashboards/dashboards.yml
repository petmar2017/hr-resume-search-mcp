# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: 'HR Resume Search'
    orgId: 1
    folder: 'HR Resume Search'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards